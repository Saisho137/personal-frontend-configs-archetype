/**
 * Archivo de ejemplo para validación de TypeScript.
 *
 * Este archivo existe únicamente para que `tsc --noEmit` no falle
 * cuando el archetype no tiene código fuente.
 *
 * Elimínalo o reemplázalo cuando agregues tu código real.
 */

export const ARCHETYPE_VERSION = '1.0.0';
